---
- name: Linux Reboot Demo
  hosts: all
  gather_facts: no
  remote_user: ansible
  become: true
 
  tasks:
    - name: Reboot the machine (Wait for 10 second)
      reboot:
        reboot_timeout: 10
