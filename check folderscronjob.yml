---
- hosts: all
  gather_facts: no

  tasks:
  - name: Ensure a job that runs at 2 and 5 exists. Creates an entry like "0 5,2 * * ls -alh > /dev/null"
  cron:
    name: "check dirs"
    minute: {{ M_TIME }}
    hour: {{ M_HOUR }}
    job: "ls -alh > /dev/null"
    register: M_TIME
              M_HOUR
